# 새 저장소 연결 절차

## 주소 변경

새 서비스 URL: `https://kokoom94-ai.github.io/jeju-oldtown-3d/`
이전 안내 URL: `https://kokoom94-ai.github.io/jeju-now-981/`

저장소 이전으로 경로가 바뀌었습니다. 키 원문을 보내거나 먼저 재발급하지 말고, 브이월드 인증키 관리에서 등록 서비스 URL을 새 주소와 일치시키세요. 발급기관 관리 화면에 수정 기능이 없다면 공식 안내를 확인해야 합니다. 도메인만 같다고 인증 성공을 단정하지 않습니다.

## 최초 연결

1. 새 저장소 Pages 설정: Deploy from a branch → jeju-precision-site → /(root) → Save.
2. GitHub의 게시 성공 표시와 새 URL에서 PRECISION 3.3 화면이 열리는지 확인.
3. 등록 서비스 URL·WebGL 3D 권한 확인 후 새 사이트에서 `직접 연결` 선택.
4. 발급받은 JavaScript 공개 키 입력 → 확인란 체크 → 실제 3D 연결.
5. 관덕정·동문시장 주변 등 대표 지점으로 이동해 원본 객체 선택. 6개 지점에서 표본을 기록하고 진단 JSON 내보내기.

`rawcdn.githack.com` 미리보기에서는 키 입력과 프록시 연결을 모두 막습니다. 이곳은 기능 확인용입니다. 직접 입력 키는 앱 저장소·진단 파일·페이지 URL에 보관하지 않습니다. 제공기관 SDK·브라우저 요청에서는 키가 보일 수 있습니다.

## 연결 실패 구분

| 증상 | 확인할 항목 |
| --- | --- |
| 사이트 404 | Pages 활성화, 선택 브랜치, 게시 완료 여부 |
| 키 입력 비활성 | 공유 미리보기 또는 잘못된 저장소 경로인지 확인 |
| SDK 요청 실패 | 네트워크, 브라우저 차단, HTTPS 응답 |
| SDK 지도 객체 없음 | 키·등록 URL·WebGL 3D 권한·제공기관 응답 |
| 뷰어는 열리나 건물 없음 | 해당 지역의 제공 범위·로딩·레이어 상태. 가짜 건물로 대체하지 않음 |
| 건물 보이나 선택 안 됨 | 제공 SDK의 피처 타입 호환성. 현재 선택 어댑터는 Cesium3DTileFeature만 수용 |

진단 JSON에는 성공 여부를 과장하지 않는 상태값을 기록합니다. 키·스크린샷 속 키는 공유하지 마세요.

## 선택적 프록시

`proxy-server.mjs`와 `render.proxy.yaml`을 보존했지만 별도 서버를 생성하거나 배포하지 않았습니다. 초기 0원 기본 연결에 프록시는 필요하지 않습니다.

필요할 때만 본인 서버 환경변수 `VWORLD_API_KEY` 및 `VWORLD_SERVICE_URL`을 설정합니다. 프록시 허용 주소 `https://jeju-oldtown-3d-proxy.onrender.com`은 **예정 문자열이며 확보·배포를 의미하지 않습니다**. 실제 주소가 다르면 `connection.mjs` 허용 목록을 검토·수정해야 합니다. 프록시는 SDK 초기 요청만 중계하며 건물 타일 재배포·키 완전 은닉·도메인 인증 우회 기능이 아닙니다. 실제 발급키와의 호환성은 별도 검사 대상입니다.

## 완료 기준

공개 URL 응답 → 본인 키로 SDK 실초기화 → 제주 원본 객체 수신 → 실제 건물 높이·지붕·지형·좌표/고도 기준·갱신 시점 대조 → 범위/출입구/보행 검증 순으로 확인합니다. 단위 검사와 뷰어 초기화만으로 정밀지도 완성을 선언하지 않습니다.
