# JEJU:BEFORE — 다음 개발 단계 확정 인계

기준일: 2026-09-12 (Asia/Seoul). 과거 인계문의 '새 저장소가 비어 있음·이전 미완료'는 폐기한다.
이 문서는 개발 원칙과 다음 실행 순서를 확정한 것이며, 실제 제주 정밀지도 완성 선언이 아니다.
실행 시점의 브랜치·Actions·publication.json·readiness.json을 다시 읽고 이 문서보다 새로운 변경을 보존한다.

## 1. 목적과 변경 금지 범위

서비스명: JEJU:BEFORE — 제주시 원도심 3D 사전 여행지도.
원본 건물 높이·지붕·입체 형상·지형, 3인칭 도보 체험, 항공/지도 시점 전환,
관광지·공영주차장·호텔 팝업과 네이버 지도 확인 연결을 목표로 한다.
식당·카페·베이커리는 사용자 입력 영역으로 남긴다. PC·모바일 웹을 대상으로 한다.
GTA 자산이나 전투 기능은 사용하지 않는다. 추정 높이·일반 재료 이미지를 실제 제주 건물이라고 표현하지 않는다.

개발과 쓰기는 kokoom94-ai/jeju-oldtown-3d에서만 한다.
이전 kokoom94-ai/jeju-now-981의 모든 브랜치·워크플로·배포·Render·환경변수는 읽기 전용이다.
9.81 서버·SKT 데이터·인증 설정·.deployment·릴레이를 가져오거나 실행하지 않는다.
결제·유료 전환·상시 서버·유료 자원을 만들지 않는다. 기본 방식은 정적 Pages + 직접 SDK이다.

## 2. 이미 완료된 이관 — 다시 실행하지 말 것

새 저장소는 빈 저장소가 아니다. 3.3 이관 당시 확인된 기준:
- 개발 브랜치 jeju-before-web: 88f998dafe9872431fb3414f37e56c0169185750
- 배포 브랜치 jeju-precision-site: f24294d47df858542d61fc5ad092be07605d9539
- 새 main: 3589d2b2ef64987b9d6be8fcd006305e8d3620a9 (안내용, 보존)
- 구 저장소 main: c5578da4d1ba24a8341de512552db69d56c14aba
- 구 저장소 개발 브랜치: 261619af20ca9e03425f4ce5f02fad2bf0824547
- 구 저장소 배포 브랜치: 1d7da70a6e8df70e7f3fa69b898d1fe73ca9d0e2

위 SHA는 이번 수정 전 기준점이다. 현재 수정 및 자동 검사 커밋은 GitHub에서 조회한다.
임포트의 출처·허용 파일·해시는 SOURCE.json에 있다. SOURCE.json 삭제, 강제 푸시, 재초기화 금지.
scripts/import-source.py는 이미 임포트되면 종료한다. 기존 워크플로를 과거 것으로 교체하지 않는다.

소스의 index.html / real.html / realism/은 보존된 추정형 체험판이다.
정밀 연결센터는 precision/이며, 빌드 후 루트 index.html이 된다.
추정형 체험판은 배포본 legacy/real.html과 legacy/realism/에서 실행된다.
라이선스는 realism/LICENSES.txt 및 realism/vendor/THREE-LICENSE.txt를 유지한다.

## 3. 이번 수정으로 확정한 장소 데이터 경로

현행 장소 초안 원본: precision/places.json (기존 32곳의 이름·좌표·주소 유지).
검증기: precision/places.mjs. 잘못된 JSON·빈 배열·중복 ID·잘못된 좌표를 거절한다.
precision/prepare.mjs와 precision/app.mjs는 이 JSON을 사용한다.
브라우저가 ../index.html로 우회해 다른 페이지에서 장소를 찾는 동작은 제거한다.
원본 index.html은 비교용 legacy HTML 복사에만 사용하며 장소 추출 빌드 의존성은 제거한다.
옛 parsePlaces는 과거 데이터 추출·회귀검사용으로만 남아 있다.
현재 32개 제한은 검증되지 않은 데이터를 조용히 추가하지 않기 위한 검토 장치다.
나중에 추가할 때는 개수·필드·검증 상태·테스트를 함께 변경한다.

주소·좌표는 초안이다. naverVerified=false, coordinateStatus=approximate를 유지한다.
공영 여부·운영 상태·실제 출입구·건물 ID 매칭은 별도 확인이 필요하다.
네이버 버튼은 공식 지도 검색 링크이며 네이버 API 실연동이 아니다.
기존 여정·캐릭터·추가 장소 저장은 브라우저 로컬 저장이며 공용 DB가 아니다.

## 4. 실행 주소 및 현재 외부 차단 요소

예정 전용 주소: https://kokoom94-ai.github.io/jeju-oldtown-3d/
이번 감사 시작 시 GitHub has_pages=false. 이전 새 저장소 readiness 기록은 HTTP 404였다.
이번 실행의 HTTP 상태와 파일 일치는 precision/readiness.json 및 실제 Actions 로그로 확인한다.
Pages 게시 성공 전에는 예정 주소를 운영 중이라고 말하지 않는다.

키 없는 공개 미리보기의 현재 주소는 precision/publication.json의 url 필드로 확인한다.
rawcdn.githack.com은 코드·화면 확인용이며 최종 서비스가 아니다. 키 입력과 SDK 연결을 차단한다.
미리보기 200과 배포 브랜치 커밋 성공은 전용 Pages 게시 성공을 뜻하지 않는다.

사용자 계정 설정: 새 저장소 Settings → Pages → Deploy from a branch → jeju-precision-site → /(root) → Save.
GitHub 공식 안내: GITHUB_TOKEN으로 푸시한 커밋은 브랜치 기반 Pages 빌드를 자동 유발하지 않는다.
따라서 Pages 활성화 이후에도 후속 배포는 별도 Pages 실행과 HTTP/파일 해시를 검증한다.
일반 사용자 권한의 커밋 또는 공식 Pages 배포 워크플로 사용 여부는 실제 설정과 권한에 맞춰 결정한다.
별도 관리자 토큰·키를 채팅으로 요청하지 않는다.

## 5. 이미 발급된 브이월드 키

기존 등록 URL: https://kokoom94-ai.github.io/jeju-now-981/
새 사이트 URL: https://kokoom94-ai.github.io/jeju-oldtown-3d/
호스트는 같고 경로는 다르다. 사용자 키는 이미 발급되었으며 원문은 받지 않았다.
앱의 호스트/경로 허용 목록은 새 저장소 전용 경로로 변경되어 있다.
앱의 허용 정책과 제공기관 인증 정책은 별개다.

WebGL 3.0이 호스트·Origin·전체 경로 중 무엇을 대조하는지 이번 공식 공개자료 확인만으로 확정하지 못했다.
다른 2D 데이터 API의 domain 설명을 그대로 WebGL 인증 규칙으로 적용하지 않는다.
사용자는 키 관리 화면에서 기존 키의 서비스 URL·WebGL 3D 권한을 확인한다.
새 주소로 수정 가능하면 맞추되, 경로 변경만으로 재발급이 필수라고 단정하지 않는다.
전용 사이트가 열린 뒤 기존 키를 직접 입력해 실응답으로 판정한다.
계정 비밀번호·서버 비밀키·다른 서비스나 교육 예제 키는 사용하지 않는다.

앱은 직접 입력 키를 저장소·진단 JSON·페이지 URL에 기록하지 않는다.
브라우저 요청/DOM/SDK에서는 JavaScript 키가 보일 수 있다. 완전 은닉을 주장하지 않는다.
프록시는 선택적 준비 코드이며 미배포다. SDK 최초 요청 중계이지 전체 3D 타일 중계가 아니다.
프록시는 도메인 인증 우회나 키 완전 은닉을 보장하지 않는다. 초기 연결에 Render/Netlify는 필요 없다.

## 6. 실제 구현과 미구현을 분리

연결센터 구현: 직접 키 입력, 전용 주소 제한, 공유 CDN 차단, 상태·오류 표시,
6개 지역 대표점, 장소 32곳 검색·팝업, 원본 객체 속성 확인·표본 기록·진단 JSON.
현재 피처 선택 어댑터는 Cesium3DTileFeature만 수용한다.
건물이 보이는데 선택되지 않으면 미제공으로 단정하지 말고 공급자 런타임 타입부터 확인한다.
뷰어 초기화·객체 선택만으로 높이·지붕·정확도·전체 범위 플래그를 올리지 않는다.

정밀 연결센터에 3인칭 캐릭터·보행망·건물 충돌 방지·공항 제한구역 차단은 아직 이식하지 않았다.
이 기능이 legacy에서 동작한다고 정밀 배경에서도 구현됐다고 말하지 않는다.
정밀지도 공항 보행은 허용 보행망과 제한구역 검증 전에는 활성화하지 않는다.

## 7. 대상 범위와 완료 게이트

일도동·이도동·건입동·삼도동·용담동 및 제주국제공항 일대가 목표다.
기존 사각형 BOUNDS와 6개 카메라 대표점은 행정동 전체 포함을 증명하지 않는다.
1,700m 표본 근접 검사는 먼 곳의 객체 오기록 방지일 뿐 행정구역 판정이 아니다.
행정동 경계·공항 경계·보행 허용망·제한구역 폴리곤은 별도 출처·시점·정확도 검증이 필요하다.
공항은 표시하되 활주로·계류장·출입 제한구역은 보행에서 제외한다.

G1 게시: 전용 URL HTTP 200, JS/JSON/legacy 하위 자원 로드, 현재 파일 해시 일치.
G2 인증: 기존 발급키로 WebGL 3.0 실초기화, 인증/네트워크/렌더 오류 여부 확인.
G3 원본: 관덕정·동문시장·산지천·탑동·공항 주변에서 실제 모델과 지형 수신 확인.
G4 정확도: 건물별 원본 식별자·높이 단위·수직 기준·지붕·위치·누락·구축/갱신 시점 대조.
G5 보행: 검증된 지면/보행망·충돌·경사·계단·추적 카메라·제한구역 처리 후에만 시범 공개.
G6 관광정보: 시설 최신 정보·실제 건물·출입구 매칭 및 PC/실제 모바일 성능 확인.

G1이 막혔다고 G3/G4를 모의 성공으로 채우지 않는다. G3 이전에 캐릭터 통합을 완료로 선언하지 않는다.
부족한 원본은 GIS건물통합정보·건축물대장·국토지리정보원 자료의 보유/제공/이용조건을 검토한다.
무료 API 사용 가능과 원본 다운로드·외부 공개·재배포 허용은 별개의 조건이다.

## 8. 테스트와 다음 실행 순서

Node.js 22: npm test → npm run build → npm run serve.
기존 새 저장소 3.3 실행 34665491548은 단위 60 / 화면 28 / 프록시 오류·취소 13 검사 기록이다.
이번 수정은 precision/places.test.mjs의 독립 JSON 회귀검사를 추가한다.
이번 커밋의 실제 테스트 수·통과·오류는 그 커밋의 Actions 및 아티팩트를 확인한다.
과거 3.1/3.2/3.3 성공 숫자를 새 실행 결과로 재사용하지 않는다.
모의 HTTP fixture와 로컬 호스트/공유호스트 테스트는 실제 SDK·모델 수신을 증명하지 않는다.

다음 작업은 재이관이 아니라 다음 순서다.
1. 현행 브랜치 SHA와 작업 중인 변경을 읽고 보존.
2. Pages 계정 설정 및 전용 주소/자산 로드를 확인. 키 없는 공유 미리보기와 명확히 구분.
3. 기존 키의 등록 정보 확인 후 전용 사이트에서 직접 연결. 키는 받지 않음.
4. 원본 수신·피처 타입·관덕정 등 5개 시범 지점 진단. 키 없는 진단 JSON과 화면만 공유.
5. G4 정확도·범위·시점 대조 뒤 안전한 시범 도보 구간에 3인칭 기능 통합.

매번 보고: 실제 실행 주소/접속 여부, 작업 브랜치/커밋/수정 파일, 구 저장소 무변경 여부,
이번 테스트와 미수행 항목, SDK·모델 수신 상태, 최소 계정 설정.
현장 높이·지붕·실기기 성능·행정경계 전체·공항 제한구역 검증은 아직 완료가 아니다.

## 근거 위치

현행 저장소: https://github.com/kokoom94-ai/jeju-oldtown-3d/tree/jeju-before-web
과거 이관 검사: https://github.com/kokoom94-ai/jeju-oldtown-3d/actions/runs/34665491548
Pages 공식: https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site
브이월드 공식 예제: https://github.com/V-world/V-world_API_sample
이관 명세 SOURCE.json / precision/CONNECT.md / precision/publication.json / precision/readiness.json.
